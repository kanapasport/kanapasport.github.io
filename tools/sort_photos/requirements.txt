# Zakladni - bez techto to nepobezi
pillow>=10.0
pytesseract>=0.3.10

# HEIC fotky z iPhonu (mate je, takze potreba)
pillow-heif>=0.16

# Excel reporty
openpyxl>=3.1

# Detekce a orez displeje tabletu - vyrazne zvysuje presnost cteni
opencv-python-headless>=4.9
numpy>=1.24

# Volitelne - jen pokud budete pouzivat --vision-fallback (placene Claude API)
# anthropic>=0.40
